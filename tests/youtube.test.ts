import { beforeAll, expect, test } from "vitest";
import "dotenv/config";

beforeAll(async () => {});

test("should extract captions for a given video ID", async () => {}); // Timeout for this test

test("should throw an error for a video ID without captions", async () => {});
